version: '3'
networks:
  cl:
    external: true
services:
  zookeeper:
    networks:
      - cl
    image: zookeeper:3.8.1
    container_name: zookeeper
    ports:
      - "2181:2181"
    environment:
      JVMFLAGS: "-Xmx1024m"
